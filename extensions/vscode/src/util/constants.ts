export { EXTENSION_ID } from "core/util/constants";

/**
 * The settings/command namespace for this extension.
 *
 * Single source of truth for the `fim.*` prefix. If you rename the extension,
 * change this and the matching keys in package.json.
 */
export const EXTENSION_NAME = "fim";
